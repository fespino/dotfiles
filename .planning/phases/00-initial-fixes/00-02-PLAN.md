---
phase: 00-initial-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .pre-commit-config.yaml
autonomous: true

must_haves:
  truths:
    - "Pre-commit hooks activate on git commit"
    - "Gitleaks scans for secrets before allowing commits"
    - "Commits containing secrets are blocked"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Pre-commit hook configuration with gitleaks"
      min_lines: 5
  key_links:
    - from: ".git/hooks/pre-commit"
      to: "pre-commit framework"
      via: "pre-commit install"
      pattern: "pre-commit"
---

<objective>
Install pre-commit framework with gitleaks hook for secret scanning on every commit.

Purpose: Prevent accidental commits of secrets (API keys, passwords, private keys) by scanning all staged changes.
Output: .pre-commit-config.yaml and installed git hooks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-initial-fixes/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pre-commit configuration</name>
  <files>.pre-commit-config.yaml</files>
  <action>
Create .pre-commit-config.yaml at repository root with gitleaks hook:

```yaml
# Pre-commit hooks for dotfiles repository
# Run `pre-commit install` after cloning to activate

repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.24.2
    hooks:
      - id: gitleaks
```

This configuration:
- Uses gitleaks v8.24.2 (latest stable per research)
- Scans all staged files for 160+ secret types
- Uses entropy detection, not just pattern matching
  </action>
  <verify>
File exists: `ls -la .pre-commit-config.yaml`
Contains gitleaks: `grep "gitleaks" .pre-commit-config.yaml`
  </verify>
  <done>.pre-commit-config.yaml exists with gitleaks hook at v8.24.2</done>
</task>

<task type="auto">
  <name>Task 2: Install pre-commit hooks</name>
  <files></files>
  <action>
Install pre-commit framework and activate hooks in the repository:

1. Check if pre-commit is installed: `command -v pre-commit`
2. If not installed:
   - macOS: `brew install pre-commit` (if brew available)
   - Otherwise: `pip install pre-commit` or `pip3 install pre-commit`
3. Install hooks into repository: `pre-commit install`
4. Verify hooks are active: `ls -la .git/hooks/pre-commit`

Note: The user may need to install pre-commit first if not available. Check and provide instructions.
  </action>
  <verify>
Pre-commit installed: `command -v pre-commit`
Hook file exists: `ls -la .git/hooks/pre-commit`
Hook contains pre-commit: `grep -l "pre-commit" .git/hooks/pre-commit`
  </verify>
  <done>Pre-commit hooks installed and active in .git/hooks/</done>
</task>

<task type="auto">
  <name>Task 3: Test secret scanning works</name>
  <files></files>
  <action>
Verify gitleaks is working by running pre-commit manually:

1. Run all hooks on staged files: `pre-commit run --all-files`
2. This downloads gitleaks if needed and scans the repository
3. Should pass (no secrets currently in repo)

If gitleaks finds issues:
- Check if they are false positives
- If false positives in docs/examples, note for future .gitleaks.toml

Do NOT create test secret files - just verify the hook runs.
  </action>
  <verify>
`pre-commit run --all-files` completes without error (exit 0 or reports no secrets)
  </verify>
  <done>Gitleaks hook runs successfully and scans for secrets</done>
</task>

</tasks>

<verification>
1. .pre-commit-config.yaml exists: `cat .pre-commit-config.yaml`
2. Pre-commit installed: `pre-commit --version`
3. Hooks active: `cat .git/hooks/pre-commit | head -5`
4. Gitleaks runs: `pre-commit run gitleaks --all-files`
5. Integration test: Stage a file, attempt commit, see hook output
</verification>

<success_criteria>
- .pre-commit-config.yaml exists with gitleaks hook configuration
- Pre-commit framework installed on the system
- Git hooks installed in .git/hooks/pre-commit
- `pre-commit run --all-files` executes without errors
- Future commits will be scanned for secrets automatically
- Requirement INIT-04 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/00-initial-fixes/00-02-SUMMARY.md`
</output>
